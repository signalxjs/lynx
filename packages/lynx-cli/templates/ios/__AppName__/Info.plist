<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>
    <key>CFBundleDisplayName</key>
    <string>{{appName}}</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>{{versionName}}</string>
    <key>CFBundleVersion</key>
    <string>{{buildNumber}}</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <false/>
    </dict>
    <!-- {{LAUNCH_SCREEN}} -->
    <!-- {{ORIENTATIONS}} -->
    <!-- Let @sigx/lynx-appearance.setStatusBarStyle drive the status-bar
         tint via UIApplication.setStatusBarStyle. SwiftUI hosts can't
         easily override UIHostingController's preferredStatusBarStyle, so
         this is the path of least resistance — see
         packages/lynx-appearance/ios/AppearanceModule.swift. Apps that
         prefer VC-controlled style can override this key to <true/>. -->
    <key>UIViewControllerBasedStatusBarAppearance</key>
    <false/>
    <!-- ATS: allow plaintext HTTP only to local/LAN addresses (dev server).
         NSAllowsArbitraryLoads is deliberately NOT set — it would flag release
         builds during App Store review. -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSAllowsLocalNetworking</key>
        <true/>
    </dict>
    <!-- {{USAGE_DESCRIPTIONS}} -->
    <!-- {{BACKGROUND_MODES}} -->
    <!-- {{BG_TASK_IDENTIFIERS}} -->
    <!-- {{URL_SCHEMES}} -->
    <!-- {{RUNTIME_VERSION}} -->
</dict>
</plist>
