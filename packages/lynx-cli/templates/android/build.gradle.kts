// Managed by sigx prebuild — regenerated on every prebuild with the Android
// Gradle plugin and Kotlin versions @sigx/lynx-cli is tested against. App
// build logic belongs in app/build.gradle.kts.
plugins {
    alias(libs.plugins.android.application) apply false
    // Not applied anywhere: AGP 9 compiles Kotlin itself (built-in Kotlin).
    // Declaring it pins the Kotlin version that uses.
    alias(libs.plugins.kotlin.android) apply false
    alias(libs.plugins.kotlin.compose) apply false
    alias(libs.plugins.legacy.kapt) apply false
}
