/**
 * Transform-contract surface (`@sigx/lynx/internal`) — the module compiled
 * snapshot registrations import (the build plugin points the upstream
 * transform's `runtimePkg` here). NOT public API: everything is re-exported
 * from `@sigx/lynx-runtime-internal/snapshot`, whose docs are canonical.
 */

export * from '@sigx/lynx-runtime-internal/snapshot';
